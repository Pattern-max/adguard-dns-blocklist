name: Optimize Custom NetBlocker List

on:
  schedule:
    - cron: '0 2 * * 1'  # 每周一UTC 2点（北京时间10点）运行
  workflow_dispatch:      # 支持手动触发
  push:
    branches: [ main ]

jobs:
  optimize:
    runs-on: ubuntu-latest
    permissions:
      contents: write    # 关键：授予写入仓库的权限

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install cleaner-adblock tool
        run: |
          git clone https://github.com/ryanbr/cleaner-adblock.git
          cd cleaner-adblock
          npm install puppeteer

      - name: Download NetBlocker List
        run: |
          # 下载您的自定义规则列表
          curl -sSL "https://raw.githubusercontent.com/Pattern-max/NetBlocker/main/list/NetBlocker.txt" -o NetBlocker-raw.txt
          echo "📥 原始规则数统计："
          echo "  NetBlocker: $(grep -c '^[^!#]' NetBlocker-raw.txt) 条规则"

      - name: Optimize List with Cleaner-adblock
        run: |
          cd cleaner-adblock
          echo "🔧 开始优化 NetBlocker 规则列表..."
          # 优化 NetBlocker 列表
          if [ -f ../NetBlocker-raw.txt ]; then
            node cleaner-adblock.js ../NetBlocker-raw.txt --export-list --check-dig-always --ignore-similar --concurrency=6
            # 查找并移动优化后的文件
            find . -maxdepth 1 -name "*NetBlocker-raw_cleaned_*.txt" -exec mv {} ../NetBlocker_optimized.txt \;
          fi
          echo "✅ 优化完成。"

      - name: Generate Final AdGuard-compliant Rules
        run: |
          # 生成最终纯净规则文件
          if [ -f "NetBlocker_optimized.txt" ]; then
            # 移除注释和空行，生成最终文件
            grep -v -e '^!' -e '^#' -e '^$' "NetBlocker_optimized.txt" > "NetBlocker_final.txt"
            # 统计行数
            original_count=$(grep -c '^[^!#]' NetBlocker-raw.txt)
            final_count=$(wc -l < "NetBlocker_final.txt")
            echo "📊 规则数统计："
            echo "  原始规则: $original_count 条"
            echo "  优化后规则: $final_count 条"
            echo "  移除规则: $((original_count - final_count)) 条"
          else
            echo "⚠️  未找到优化后的文件，将直接处理原始文件。"
            grep -v -e '^!' -e '^#' -e '^$' "NetBlocker-raw.txt" > "NetBlocker_final.txt"
          fi

          # 清理中间文件
          rm -f NetBlocker-raw.txt NetBlocker_optimized.txt 2>/dev/null || true

      - name: Commit Optimized File to Repository
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # 添加最终规则文件
          git add NetBlocker_final.txt
          if git diff --staged --quiet; then
            echo "📭 无新变更，跳过提交。"
          else
            git commit -m "Auto-optimize: NetBlocker List $(date '+%Y-%m-%d')"
            git push
            echo "✅ 优化后的 NetBlocker 规则文件已提交至仓库。"
          fi
